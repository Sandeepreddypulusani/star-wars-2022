import { styled } from "@mui/system";

export const Header1 = styled("h1")({
    color: "#fff",
    textAlign: "center"
})

export const Header2 = styled("h2")({
    color: "#fff",
    textAlign: "center"
})